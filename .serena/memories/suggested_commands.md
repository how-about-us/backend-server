# Suggested Commands\n- `./gradlew bootRun --args='--spring.profiles.active=dev'` : run locally with the dev profile. Requires `.env.dev`.\n- `./gradlew test` : run tests.\n- `./gradlew build` : full local verification before review/PR.\n- `./gradlew bootJar` : build the runnable jar.\n- `docker compose -f compose.yaml -f compose.dev.yaml up -d` : start local infra manually.\n- `docker compose -f compose.yaml -f compose.dev.yaml down` : stop local infra manually.\n- `rg <pattern>` / `rg --files` : preferred fast text/file search in this repo.\n- `git status`, `git diff`, `git add`, `git commit` : normal Git workflow.\n- Standard Linux utilities available and useful here: `ls`, `cd`, `find`, `sed`, `cat`.\n- No dedicated formatter or linter command was identified in the inspected project files.