# Task Completion Checklist\n- If the task changes behavior, API, DB, or policy, update the relevant docs under `docs/ai/` according to `AGENTS.md` (`features.md`, `erd.md`, decision records as needed).\n- Before review/PR, verify the code still matches the docs.\n- Run `./gradlew build` before opening a PR; at minimum run the relevant tests.\n- When reporting completion, separate what was verified from what was not verified.\n- Do not push to `main`; push/PR creation/dependency addition/DB schema changes require explicit confirmation.\n- Commit messages follow Conventional Commits, and the one-line summary topic should be written in Korean.\n- Do not add `Co-Authored-By:` trailers when committing as an AI agent.